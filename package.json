{
  "name": "snap",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "type-check": "turbo type-check",
    "clean": "turbo clean",
    "format:check": "biome check .",
    "format": "biome check . --write",
    "ui:add": "pnpm --filter @repo/ui ui:add",
    "ui:diff": "pnpm --filter @repo/ui ui:diff",
    "manypkg:fix": "manypkg fix",
    "postinstall": "manypkg check"
  },
  "dependencies": {
    "@biomejs/biome": "1.8.3",
    "@manypkg/cli": "^0.21.4",
    "prettier": "^3.3.3",
    "turbo": "^2.0.11",
    "typescript": "^5.5.4"
  },
  "packageManager": "pnpm@9.6.0",
  "engines": {
    "node": ">=20"
  }
}
